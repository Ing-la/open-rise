# 开发日志

## 维护规则

1. **每次记录以真实日期时间开头**，格式：`## YYYY-MM-DD HH:mm [作者标识]`（如 `## 2026-04-30 23:10 [02-UI]`），作者标识即写这条日志的人。**不知道 session 名且用户没主动给出时，停下来问用户**，不要自己编。用 `date +"%Y-%m-%d %H:%M"` 获取真实时间
2. **言简意赅**，记关键决策、进展、问题，不罗嗦
3. 新增条目写在**最前面**（倒序）
4. **永不修改已有条目**。每次变更写新条目，不是把新内容塞进旧条目里
5. **每次更新日志时，检查功能文档（`0-` 到 `4-`）是否需要同步更新**

---




## 2026-05-09 17:20 [05-feature]

### 百炼 DashScope 文生图适配器 + 下载按钮 + 图片缩放

**DashScope 适配器：**
- `chat:send-stream` 图片分支增加 DashScope 检测（`brain.baseUrl.includes('dashscope')`）
- DashScope 走专有端点 `POST /api/v1/services/aigc/multimodal-generation/generation`，非 OpenAI 格式
- 请求体差异：`{ model, input: { messages: [{ content: [{ text }] }] }, parameters }` 而非 `{ model, prompt, size }`
- 响应体差异：图片 URL 在 `output.choices[].message.content[].image`，非 `data[].url`
- 状态码差异：百炼将状态码放在 JSON 体内（`data.status_code`），独立于 HTTP status
- 提取 `rawHttps()` 通用请求函数，消除 `https.request` 模板代码重复
- `isImageModel()` 新增 `qwen` 关键词

**下载按钮：**
- 图片右上角新增下载按钮（hover 显示），点击弹系统原生保存对话框
- IPC 链路：`ChatView` → `file.saveImage` → `file:save-dialog` → `dialog.showSaveDialog` + `fs.copyFileSync`
- 新增 `preload.js` → `file` 模块

**图片缩放：**
- ChatView 图片渲染加 `max-h-64`，等比缩小不撑爆

**文档同步：** 4-文生图功能.md 更新为双协议路由说明

---

## 2026-05-09 16:30 [05-feature]

### 自定义协议 `app-img://` 替代 `file://` 加载本地图片

**动机：** `file://` 在 Windows 上路径格式易错（反斜杠/盘符问题），dev 模式存在跨协议安全限制（http 页面不让加载 file://）。改用 Electron 原生自定义协议统一处理。

**变更：**
- `main/main.js`：注册 `app-img` 特权协议，`protocol.handle` 将 `app-img://` 请求转发为 `file://` 加载
- 图片存储路径改为 `app-img:///{absolutePath}`（正斜杠，跨平台）
- `ChatView.tsx`：`<img src={msg.content}>` 直接使用协议 URL，不再拼 `file://` 前缀
- 图片下载逻辑不变，仍存 `prisma/images/{roleId}/{timestamp}.png`

**关键决策：**
- `protocol.registerSchemesAsPrivileged` 带 `bypassCSP/stream/supportFetchAPI` 权限，避免安全拦截
- 协议 handler 简单替换 `app-img:` → `file:` 后 `net.fetch()`，不做路径拼接，避免跨平台差异
- 旧消息的 `content` 仍为绝对路径，暂不兼容（只影响已生成的旧图）

---

## 2026-05-09 15:48 [05-feature]

### Brain 新增 type 字段：手动标记路由取代纯关键词检测

**动机：** 关键词匹配不可穷举——新模型、改名模型都会漏。用户配 API 时本来就清楚模型功能，加一个显式标记更可靠。

**变更：**
- `prisma/schema.prisma`：Brain 表新增 `type String @default("chat")`（"chat" | "image"）
- `BrainModal.tsx`：模型输入行末尾增加「对话 / 文生图」两个切换按钮，必选后才能保存。所有必要字段（名称/供应商/地址/Key/模型/类型）均需填写
- `main/main.js`：`chat:send-stream` 改为 `brain.type === 'image' || isImageModel(brain.modelName)`，手动标记优先，关键词匹配降级为兜底
- `brain:create/update` 接受并存储 `type` 参数
- `api.ts` / `ipc-contracts.js` 同步更新参数类型

**关键决策：**
- 手动标记优先 + 关键词兜底，兼容老数据
- 多模态模型选主要输出方式标记（如输出图片+文字的标记为 image）
- 类型按钮放在模型输入行末尾，不单独成行，节省空间

---

## 2026-05-09 14:56 [05-feature]

### 文生图扩展：多厂商模型名支持 + BrainModal 布局调整

**模型检测扩展：**
- `isImageModel()` 新增关键词：`cogview`（智谱GLM）、`wanx` / `taiyi`（百炼通义万相）、`midjourney`
- 去掉过于宽泛的 `image` 和 `gen` 关键词，避免误匹配
- 底层 API 调用格式不变（`/v1/images/generations`），三厂商均支持 OpenAI 兼容格式，无需额外适配器

**UI 调整：**
- BrainModal 取消/保存按钮从表单底部移到标题行右上角，弹窗整体高度缩短
- 对应更新 `4-文生图功能.md` 文档

---

## 2026-05-09 13:52 [05-feature]

### 文生图重构：去掉显式类型标记，改为模型名自动检测

**动机：** Brain 的 `type` 字段（"chat"/"image"）是多余的——用户配置 API 和选模型时已知功能。拆分成 chat/image 两套界面不合理，未来文生视频、图生文等多模态场景不可扩展。

**变更：**
- `Brain` 表去掉 `type` 字段（仅保留 `Message.type` 用于前端渲染）
- 删除 `ImageView` 组件和所有 image 相关 IPC（`image:generate/result/error`）
- `chat:send-stream` 中新增 `isImageModel()` 模型名检测函数，匹配关键词（flux/stable-diffusion/dall-e 等）时自动调用 `/v1/images/generations` 接口，返回结果存为 `type: 'image'` 的 Message
- `ChatView` 新增 `msg.type === 'image'` 渲染分支，显示 `<img>` 标签
- `BrainModal` 去掉类型切换按钮，恢复简洁表单
- `PageShell`/`CommandCenter` 去掉 image mode 和 `/image` 命令
- 所有 brain CRUD 去掉 `type` 参数

**设计原则：**
- 用户选择模型型号即决定路由，无需额外标记
- ChatView 统一处理所有消息类型，按 `Message.type` 区分渲染
- 后端自动检测，前端无感

---

## 2026-05-09 13:31 [04-sql]

### 项目文档重构：按功能拆分

旧的按技术层拆分（项目设计 / UI / 数据库 / 后端逻辑）重构为按功能组织：

| 新文档 | 内容来源 |
|--------|----------|
| `0-全局设计.md` | 技术栈、视觉规范、数据模型、架构图、文件结构 |
| `1-命令驱动交互.md` | 命令系统、首页布局、PageShell、CommandCenter |
| `2-聊天功能.md` | ChatView UI、IPC 流式流程、记忆压缩参数 |
| `3-大脑与角色管理.md` | BrainModal、RoleModal、CRUD handler、api.ts |
| `4-文生图功能.md` | ImageView、image 类型大脑、图片存储（原文档不变） |

删除旧文件：`项目设计.md`、`ui-设计.md`、`数据库.md`、`后端逻辑.md`、`Gemini 沟通.md`。
更新日志维护规则 5。

## 2026-05-09 13:28 [05-feature]

### 文生图功能实现

**新增：**
- `Brain` 表新增 `type` 字段（"chat" | "image"），`Message` 表新增 `type` 字段（"text" | "image"）
- 新建 `ImageView` 组件：输入 prompt → 发送 → 接收图片本地路径 → `<img>` 渲染（`file://` 协议），支持历史记录
- 新增 `image:generate` IPC handler（main.js），调用 `/v1/images/generations` 接口，下载图片到 `prisma/images/{roleId}/{timestamp}.png`，保存绝对路径到 Message

**改动：**
- `shared/ipc-contracts.js`：新增 IMAGE 区块 + preload 映射（`sendGenerate` / `onResult` / `onError`）
- `main/preload.js`：新增 image 模块（send + 事件监听 + cleanup）
- `src/lib/api.ts`：新增 `generateImage` 函数 + `OpenRiseAPI.image` 接口 + brain 参数添加 `type`
- `main/main.js`：`brain:create/update` 支持 `type` 参数；`role:list` 返回 `brainType`
- `BrainModal`：新增硅基流动预设、类型切换按钮（对话 / 文生图）
- `PageShell`：mode 新增 `'image'`，`/image` 命令路由，选人时根据 `brainType` 自动切换 chat/image 模式
- `.gitignore`：添加 `prisma/images/`

**关键设计决策：**
- 图片存储方式：存本地绝对路径，前端通过 `file://` 协议加载
- 路由策略：role 的 `brainType` 决定点击后进入 chat 还是 image 模式
- 不处理多图生成（n > 1）、非硅基流动厂商、流式

---

## 2026-05-08 18:47 [04-sql]

### Chat 功能修复收尾 + 记忆策略文档补全

**Bug 修复：**
- 修复 `chat:list` 重复注册导致 Electron 崩溃（流式改造残留的重复 handler）
- 修复 `compressMessages` 中 `??` + `||` 混用语法错误，加括号明确优先级
- 修复流式输出消息 ID 碰撞（`Date.now()` → 全局递增计数器 `msgIdCounter`）
- 修复第二次回答内容串到第一条的 bug（ID 碰撞 + 流完成后未刷新消息列表）
  - `onDone` 后调用 `listMessages` 从 DB 重载，替换乐观 ID 为真实 ID

**清理：**
- 去掉 `chat:list` 的 `take: 30` 硬限制（与压缩策略冲突，由压缩兜底）
- ChatView 切换人物时增加 loading 占位（"加载中..."动画）

**文档：**
- `4-记忆管理策略.md` 新增「压缩提示词（Prompt）」章节，包含完整 prompt、各组件作用、设计要点、信息传递链路

### 流式输出实现：SSE → IPC 事件推送

**后端（main/main.js）：**
- `chat:send`（ipcMain.handle，非流式）替换为 `chat:send-stream`（ipcMain.on，流式）
- 请求 LLM API 时加 `stream: true`，通过 `response.body.getReader()` 读取 SSE 流
- 每个 delta 块通过 `event.sender.send('chat:chunk', { content })` 推送到前端
- 流结束保存完整回复到 DB，发送 `chat:done`
- 异常时发送 `chat:error`

**桥接（preload.js）：**
- `chat.send`（invoke）替换为 `chat.sendStream`（send）+ `onChunk/onDone/onError`（事件监听）
- 每个监听函数返回取消监听的函数，支持调用方主动清理

**前端（api.ts + ChatView.tsx）：**
- `sendChatMessage` 替换为 `sendChatMessageStream(roleId, content, callbacks)`
- ChatView：`streamCleanupRef` 管理生命周期，组件卸载时自动取消监听
- 发送时乐观展示用户消息 + 空白 AI 占位，chunk 逐字追加内容
- 错误时撤回两条消息 + 显示错误提示
- 项目设计.md：路线图"流式输出"标记完成，IPC 表更新

---

## 2026-05-08 16:28 [04-sql]

### 文档拆分：策略设计 + 代码解析独立文档

- `4-记忆管理策略.md`：去掉"代码实现"章节，专注设计层面（流程、参数、预算）
- 新建 `4-记忆管理代码解析.md`：面向 Python 背景读者的 JS 逐行详解，覆盖：
  - JS 基础速查表（require/async/箭头函数/模板字符串/解构赋值等，每处对比 Python 写法）
  - chat:send / compressMessages / checkAndCompress 三段核心函数拆解
  - Prisma schema summary 字段说明
  - 函数调用关系总图
- `4-记忆管理策略.md` 末尾添加交叉引用链接指向代码解析文档

---

## 2026-05-08 16:03 [04-sql]

### 压缩逻辑定稿：字符阈值直算 + 参数文档化 + 代码章节补充

**阈值简化：**
- 去掉 `CHARS_PER_TOKEN` 和 `COMPRESS_TOKEN_THRESHOLD`，改为直接 `COMPRESS_CHAR_THRESHOLD = 20000 chars`
- `SUMMARY_MAX_TOKENS` 从 1024 提升到 2048，压缩比更合理
- `chat:send` 去掉 `take: 30`，改为取全部消息（阈值自控，无需硬限制）

**文档同步（规则 5）：**
- `4-记忆管理策略.md`：新增"代码实现"章节，列出 `main/main.js` 四段核心代码（常量 / chat:send / compressMessages / checkAndCompress）及 `schema.prisma` summary 字段，每段标注行号和完整注释
- `数据库.md`、`后端逻辑.md`、`项目设计.md`：清除所有残留的"30条/20条"表述

**参数总结：**

| 参数 | 值 | 位置 |
|------|----|------|
| COMPRESS_CHAR_THRESHOLD | 20000 chars | main/main.js:210 |
| SUMMARY_MAX_TOKENS | 2048 | main/main.js:211 |
| SHORT_TERM_KEEP | 10 条 | main/main.js:212 |

---

## 2026-05-08 13:50 [04-sql]

### 修正压缩逻辑：保留最近 10 条短期记忆，压缩其余全部早期对话

**问题：** 原逻辑只取最早的 20 条压缩，导致早期消息不断积压。

**修正：**
- 改为保留最近 10 条作为短期记忆，其余全部压缩归档
- 去掉 `BATCH_SIZE` 常量，改用 `SHORT_TERM_KEEP = 10`
- `prisma/archives/` 目录已创建（含 `.gitkeep`），压缩触发时自动写入 markdown

**文档同步：**
- `4-记忆管理策略.md`：压缩流程、超参数、关键描述同步更新

---

## 2026-05-08 13:43 [04-sql]

### 记忆方案定稿：summary 字段 + token 阈值压缩 + markdown 归档（规则 5 同步）

### 记忆方案定稿：summary 字段 + token 阈值压缩 + markdown 归档（规则 5 同步）

**设计变更：**
- 去掉 Memory 表和相关 IPC handler，Role 新增 `summary String?` 字段
- 长期记忆 = Role.summary（一段 LLM 压缩的文本），始终拼入 system prompt
- 不再需要索引层和 read_memory 工具，summary 直接参与对话

**Token 阈值压缩：**
- 使用 ~4 chars/token 启发式估算，不依赖外部分词库
- 阈值 3000 tokens（~12000 chars），超过时异步压缩最早 20 条
- 压缩使用角色绑定的同一大脑配置，fire-and-forget 不阻塞对话
- 旧 summary + 20 条对话 → LLM → 新 summary

**历史归档：**
- 压缩的 20 条原始对话以 markdown 文件保存到 `prisma/archives/`
- 格式：角色名-时间戳.md，包含角色名、时间戳、summary 快照、完整对话

**代码变更：**
- `prisma/schema.prisma`：移除 Memory 模型，Role 新增 summary
- `main/main.js`：去掉 memory:read/update/clear handler，chat:send 增加 summary 拼入 + 异步压缩
- `main/preload.js`、`src/lib/api.ts`、`shared/ipc-contracts.js`：去掉 memory 模块

**文档同步（规则 5）：**
- `数据库.md`：去掉 Memory 表，ER 图简化为 3 表，Role 新增 summary 字段
- `后端逻辑.md`：数据模型、IPC 表、handler 说明同步更新
- `项目设计.md`：数据模型、IPC 表、路线图同步更新
- `4-记忆管理策略.md`：完全重写为 summary 方案

## 2026-05-04 15:57 [02-UI]

### ChatView 输入框全局居中 + 顶部栏跟随侧边栏 + 首页 OpenRise 上移（规则 5 同步）

**输入框全局居中：**
- 站立列从 flex 子元素改为 absolute 覆盖层（`z-10 bg-paper`），主区域占满 `flex-1` 全宽
- `max-w-3xl mx-auto` 输入框相对于整个窗口居中，不再受站立列宽度影响

**顶部栏名字跟随侧边栏：**
- 侧边栏打开时，顶部栏名字 translateX 增加 `+ 128px`（半个侧边栏宽度），与输入框偏移量一致
- 加 `transition-transform duration-200 ease-out` 与侧边栏动画同步

**首页布局调整：**
- OpenRise 标题 `translateY(-40px)` 略高于垂直中心，命令行输入框保持原位不动
- `tsconfig.json` 排除 `docs/` 目录，解决预存 TS 类型检查报错

**文档同步（规则 5）：**
- `ui-设计.md`：布局表更新标题偏移+顶部栏侧边栏联动；ChatView 补充站立列 absolute 说明+输入框全局居中说明；组件树站立列标注 absolute
- `项目设计.md`：首页标题"略高于垂直中心"；站立列标注 absolute

## 2026-05-04 14:09 [01-initial]

### 项目更名：OpenRise

- 全局重命名：代码标识符 `zoeAPI` → `openriseAPI`、`zoe.db` → `openrise.db`、`ZOE` 显示文字 → `OpenRise`
- 涉及 8 个文件：preload.js、db.js、api.ts、layout.tsx、PageShell.tsx、ChatView.tsx、package.json、package-lock.json
- 同步更新 5 个 project 文档 + README.md 中的全部项目名引用
- 目录名保留 `zoe` 不变

---

## 2026-05-04 14:09 [01-initial]

### GitHub 提交前准备

- 重写 `README.md`：项目定位、技术栈、快速开始、项目结构、数据模型、命令系统说明
- 更新 `.gitignore`：添加 `*.db`、`prisma/dev.db`、`.claude/`；docs 只保留 `project/`，其余全部忽略
- 删除已重命名的 `未命名.md`（已整理为 `5-开发番外：工程问题与原理探索.md`）

---

## 2026-05-03 17:45 [04-sql]

### 记忆管理方案定稿：单文件 + 索引两层 + 代码全量重构（规则 5 同步）

**架构确认：**
- 长期记忆 = 单文件（Role 1:1 Memory），`## ` 标题提取索引，按需加载某节正文
- 短期记忆 30 条，每 20 条触发 LLM 全局合并整理（短期 + 完整旧记忆 → 全新记忆文档）
- 文档无硬性长度上限，超限时 LLM 在整理时自行压缩
- 记忆正文 + 待整理消息 ≤ 128K 即为合理

**数据库变更：**
- `schema.prisma`：移除 Individual 模型；Role 合并 soul/rule/avatar；Message 改为 roleId + sender；Memory 1:1 Role（@unique）
- 迁移已重置并重新应用（`20260503094352_role_memory_redesign`）

**后端重构：**
- `main/main.js`：`individual:xxx` → `role:xxx`，去掉所有两表操作；`chat:send` 直查 Role → Brain；新增 `memory:read/update/clear`
- `main/preload.js`：`individual` → `role`，新增 `memory` 模块

**前端同步：**
- `src/lib/api.ts`：`listIndividuals/createIndividual` → `listRoles/createRole`；新增 `readMemory/updateMemory/clearMemory`
- `RoleModal.tsx`：变量 `individuals` → `roles`，函数调用全部更新
- `ChatView.tsx`：`msg.role` → `msg.sender`（适配 schema sender 字段）
- `PageShell.tsx`：`listIndividuals` → `listRoles`
- `shared/ipc-contracts.js`：INDIVIDUAL → ROLE，新增 MEMORY 合约

**文档同步（全部已完成）：**
- `数据库.md`：ER 图、表结构、Memory 1:1 更新
- `后端逻辑.md`：handler 描述、合约表全面更新
- `项目设计.md`：数据模型、路线图、IPC 表更新
- `4-记忆管理策略.md`：三稿定稿为单文件 + 索引两层
- `ui-设计.md`：无数据库相关内容，无需更新

---

## 2026-05-02 20:55 [02-UI]

### AvatarIcon 重写：涂鸦情绪头像 + CSS 动画系统 + Markdown 渲染（规则 5 同步）

**头像重构：**
- 删除 5 个旧头像（star/wrench/leaf/moon/flame），保留 4 个（cat/ghost/crown/robot）并添加 CSS 动画
- 新增 4 个涂鸦风格情绪头像：excited（亢奋，弹跳+能量锯齿）、lazy（懒惰，下垂+打哈欠+zzz）、cool（冷酷，多边形+交叉双臂）、cute（可爱，摇摆+心形嘴+腮红）
- 新增 ANIM_STYLE 常量 + SvgFrame 包装组件，CSS keyframes：blink-s/blink-f/wave/float/bounce/sway/droop/nod/antenna-glow
- AVATARS 从 9 项调整为 8 项

**Markdown 渲染：**
- ChatView AI 回复改用 react-markdown + remark-gfm
- globals.css 新增 .markdown-content 样式（p/ul/ol/code/pre/h1-h3/a/blockquote/table/hr/strong），等宽字体+炭笔色

**文档同步（规则 5）：**
- `ui-设计.md`：AvatarIcon 描述从"9个"改为"8个涂鸦风情绪 SVG"；头像动画 TODO 标记为已完成；RoleModal 头像选择器说明同步更新；ChatView 补充 Markdown 渲染说明

## 2026-05-01 16:50 [04-sql]

### 重构：去掉 Individual，Role 作为核心实体 + 多文件记忆设计（规则 5 同步）

**数据库重构：**
- 移除 Individual 模型，soul/rule/brainId/avatar 合并到 Role
- 新增 Memory 模型（1:N Role），独立文件 + 标题 + 摘要
- Message 关联改为直接指向 Role（`individualId` → `roleId`）
- `schema.prisma`、迁移 SQL、dev.db 需重建

**文档同步：**
- `数据库.md`：ER 图、表结构、外键约束、级联策略全部重写
- `后端逻辑.md`：IPC handler 从 individual:xxx 改为 role:xxx，去掉两表操作
- `项目设计.md`：数据模型、路线图、IPC 表同步更新
- `4-记忆管理策略.md`：三层架构重写为多文件方案
- `ui-设计.md`：无数据库相关内容，无需更新

---

## 2026-05-01 16:38 [02-UI]

### ChatView 消息列表 + 聊天记录持久化 + 输入框布局修复

**消息列表：**
- ChatView 新增消息列表（切换人物时 `listMessages` 加载最近 20 条）
- 用户消息右对齐、AI 回答左对齐（左上显示 avatar 24px + 人物名字）
- Enter 发送消息（Shift+Enter 换行），发送后自动滚动到底部
- 消息与输入框同宽（`max-w-3xl mx-auto`）

**聊天记录 IPC：**
- `main/main.js` 新增 `chat:list` handler（按 individualId 查正序 20 条）
- `main/preload.js` 暴露 `chat.list`，`src/lib/api.ts` 新增 `listMessages`
- `shared/ipc-contracts.js` 补充 CHAT.LIST 合约

**输入框修复：**
- 发送消息后 `textarea.style.height` 改为 `''`（原为 `'0'`），消除高度塌缩闪缩
- 输入框放回正常 flex 流（非 absolute overlay），高度位置与改动前一致
- 构建通过

**规则 5 同步：**
- `后端逻辑.md`：preload API 补充 `chat.list`，新增 `chat:list` handler 说明
- `项目设计.md`：handler 数量 8→9，IPC 表新增 `chat:list` 行
- `ui-设计.md`：无界面变更需要同步

## 2026-05-01 15:20 [04-sql]

### 确定记忆设计方向：Individual 1:1 Memory（规则 5 同步）

- 明确设计原则：一个人物只有一份记忆（1:1 关系），而非多条记忆条目
- `prisma/schema.prisma`：Message 注释"记忆管理"→"原始对话记录"
- `数据库.md`：Message 改名为"原始对话记录"；末尾新增"设计备忘：长期记忆"章节
- `项目设计.md`：路线图"记忆管理"→"长期记忆（独立记忆表，每人一份）"
- `后端逻辑.md`：无记忆相关内容，无需更新
- `ui-设计.md`：无数据库相关内容，无需更新

---

## 2026-05-03 17:45 [04-sql]

### 新增数据库独立文档（规则 5 同步）

- 创建 `docs/project/数据库.md`，从后端逻辑中拆分出纯数据结构文档
- 完整记录 4 表字段、外键约束、级联删除策略、索引、迁移管理
- `后端逻辑.md`：数据模型和数据库方案段落实物化，改为引用数据库文档
- `项目设计.md`：修正"两层模型"旧描述，改为四层准确描述并引用数据库文档；UI 设计无需更新

---

## 2026-05-01 15:04 [03-backend]

### chat:send 全链路实现

- `main/main.js` 新增 `chat:send` handler：查 Individual+Role+Brain → 存用户消息 → 取 20 条历史 → soul+rule 拼 system prompt → fetch OpenAI 兼容 API → 存回复 → 返回
- `main/preload.js` 暴露 `chat.send`
- `src/lib/api.ts` 新增 `sendChatMessage`
- `ChatView`：消息列表、Enter 发送/Shift+Enter 换行、sending/error 状态、自动滚动、切换人物重置
- 先非流式，后续再改流式

---

## 2026-05-01 14:40 [02-UI]

### 同步设计文档（规则 5）

- `ui-设计.md`：更新首页布局表（顶部栏定位 `top-0 h-12`）、ChatView 章节补充侧边栏/站立列/门、组件树补充站立列和 AvatarIcon
- `项目设计.md`：ChatView 章节补充侧边栏+站立列描述
- `后端逻辑.md`：无界面相关内容，无需更新

## 2026-05-01 14:33 [02-UI]

### PageShell/ChatView 整体重构 + 站立列门动画 + 英文名截断修复

**布局架构变更：**
- PageShell 统一管理 mode/selectedPerson/individuals 状态，ChatView 降级为纯展示组件
- 固定顶部栏（h-12, flex items-center, px-10）：≡ 左 | 名称居中（absolute left-1/2 + translateX）| OpenRise 右
- home/chat 共享同一顶部栏，chat 模式 OpenRise 文字变为返回按钮
- 侧边栏：flex shrink-0 + width 过渡（256px↔0），chat 模式显示人物列表，点选切换对话
- ChatView 站立列（左）：w-28，门 + 站立人物列表（AvatarIcon 48px + 名字）
- 侧边栏打开时站立列隐藏（`!sidebarOpen` 控制），不用动画直接消失

**门动画交互：**
- 点击门 → 人物 translateY 向上缩进门里（或从门里下来），`pointerEvents` 同步禁用
- 错峰延迟：开门从上到下（i * 60ms），关门从下到上（reverse）
- 门 SVG 迭代：先从右侧移到左侧 → 放大 1.5x → 简化（去掉内面板，只留门框+把手）

**英文名截断修复：**
- 站立列列宽 96→112px，名字从 `max-w-[5.5rem] truncate` 改为 `w-full truncate`，撑满按钮宽度
- 顶部栏名字去掉 `truncate max-w-[320px]`，改用 `whitespace-nowrap`，彻底不截断
- 头像 28→32px，文字 `text-base`→`text-lg`，居中偏移校准 `14px`→`16px`

**其他修复：**
- 按钮添加 `focus:outline-none` 消除浏览器默认聚焦边框
- Person 列表在 enterChat 和侧边栏打开时刷新，解决 /role 新建后看不到的问题
- 站立列隐藏时 `pointerEvents: 'none'`，修复 translateY 后点击区域偏移问题
- 构建通过


## 2026-04-30 23:10 [02-UI]

### 大脑/人物卡片编辑按钮 + 大脑测试连接

- 新增 `brain:update`、`brain:test`、`individual:update` IPC handler
- BrainModal 卡片新增三个按钮：编辑（预填表单）、测试（闪电图标，调用 API /models，成功绿点/失败红点）、删除
- RoleModal 卡片新增编辑按钮（预填表单），同步新增 `individual:update`
- 所有取消/返回清除编辑状态；`+` 按钮新建时重置表单字段
- `updateBrain`、`testBrainConnection`、`updateIndividual` 加入 `src/lib/api.ts`
- 构建通过

## 2026-04-30 22:30 [02-UI]

### 全链路数据持久化（Prisma 降级 + IPC 对接）

- BrainModal / RoleModal 对接真实数据库：创建大脑、保存人物全部写入 SQLite
- 新建 `src/lib/api.ts`：客户端 API 抽象层，自动探测 Electron IPC
- `main/main.js`：添加 7 个 IPC handler（brain:list/create/delete/get, individual:list/create/delete）
- `main/preload.js`：暴露 `openriseAPI.brain.*` 和 `openriseAPI.individual.*`
- **Prisma 7 → 5 降级**：
  - Prisma 7 移除了 `datasources` 构造参数，强制要求 driver adapter
  - driver adapter 依赖原生模块（`better-sqlite3`、`@libsql/client`），在 Electron 中编译不兼容
  - 降级至 Prisma 5.22.0，恢复传统 `datasources: { db: { url } }` 直连
  - schema 补充 `url = env("DATABASE_URL")`
  - 删除 `prisma.config.ts`（Prisma 7 产物）
  - 迁移重新应用成功
- 删除 Next.js API 路由方案（`output: 'export'` 不支持 API 路由）

## 2026-04-30 17:50 [02-UI]

### 冷调素描纸 + OpenRise 炭笔颗粒感增强 + 项目设计文档更新

- 纸色再次调整：`#F5F0E8` → `#F2F2EE`（冷调素描纸灰白，用户反馈素描纸偏冷）
- 弹窗 SVG rect fill 和 `bg-white` 同步改为 `#F2F2EE`（背景一致）
- `#charcoal` 滤镜：新增 `feDisplacementMap` 轻度边缘位移（铅笔擦除感），grain 不透明度 0.15→0.25
- 项目设计.md 更新：色值表、视觉语言、页面说明、新增弹窗和头像选择器章节

---

## 2026-04-30 17:30 [02-UI]

### 炭笔素描风格重构：红墨水 → 炭笔 + 素描纸

- 背景色 #FFFFFF → #F5F0E8（暖白素描纸），纸纹噪点颗粒加大、透明度略增
- 主色 #60100B → #2C2C2C（炭灰/铅笔黑），去掉红钢笔感
- `#tremble` 滤镜新增 grain 混合层，线条从墨水抖动感改为炭笔颗粒摩擦感
- `#charcoal` 滤镜文字噪点增强
- 所有组件硬编码 `#60100B` 替换为 `#2C2C2C`（SVG 边框、按钮、图标等）
- `.shaky-line` / `.thin-scroll` 同步更新

---

## 2026-04-30 17:10 [02-UI]

### 头像重构：高饱和配色 + 粗描边 + 统一豆豆眼

- AVATARS 颜色从低饱和沉稳色全部替换为高饱和明亮色（红、青、金、蓝、绿、紫、黄、橙、粉）
- SVG 描边默认加粗至 2.5px，小尺寸下更稳健
- 有脸的 avatar（猫/幽灵/星星/火焰/机器人）统一使用 `( · · )` 豆豆眼，形成家族感
- 几何简化：去掉猫的鼻子嘴巴、幽灵的椭圆嘴、星星的 wink、火焰的内焰细节等

---

## 2026-04-30 16:50 [02-UI]

### RoleModal 新增头像选择器

- 添加 9 个手绘 SVG 头像（猫、幽灵、星星、扳手、叶子、月亮、皇冠、火焰、机器人）
- 每个头像独立配色，`url(#tremble)` 滤镜保持手绘抖动感
- 表单顶部增加头像选择按钮：点击弹出 3×3 选择器覆盖层
- 空头像占位符显示简约人物剪影（虚线圆 + 头 + 身体）
- 选中后头像显示在按钮上，关闭选择器
- 头像以 `avatarId: string` 存储

---

## 2026-04-30 16:20 [02-UI]

### BrainModal / RoleModal 布局调整 + 角色→人物改名

- BrainModal 空状态加号移到右上角，带圆圈缩小至 w-8 h-8
- RoleModal 同理：加号移至右上角（带圆圈），主体只显示"尚未配置人物"
- 全部 UI 文本 "角色" → "人物"（标题、placeholder、aria-label）
- 移除旧的 `{/* ── Title ── */}` 注释

---

## 2026-04-30 16:00 [02-UI]

### BrainModal 模型选择改为自由输入 + 预设供应商快捷按钮

- 模型选择从 `<select>` 下拉改为自由文本 `<input>`，用户手动输入模型名
- 空状态加号上方增加 4 个预设按钮：DeepSeek、智谱GLM、百炼、Kimi
- 点击预设按钮自动填入 vendor/endpoint/website，用户只需填大脑名称、API Key、模型名

---

## 2026-04-27 21:55 [02-UI]

### /chat 页面模式切换 + ChatView 改进

- `/chat` 改为直接切换页面模式，去掉 OpenRise 飞走/飞入动画
- ChatView 输入框从单行 input 改为 textarea：自动增长至 7 行，超限滚动（thin-scroll）
- 手绘圆角矩形边框随 textarea 自适应高度
- PageShell 简化：移除动画状态机，纯条件渲染

---

## 2026-04-27 21:55 [02-UI]

### IPC 合约 + /role 角色配置弹窗

- 创建 `shared/ipc-contracts.js`：前后端通信合约，定义 BRAIN / INDIVIDUAL / CHAT 通道名、参数、返回格式
- 新建 `RoleModal` 组件：空状态（+ 按钮）→ 表单（名字必填、大脑下拉选择、soul/rule 可选）
- 大脑下拉从合约定义的 IPC 通道获取，当前无数据时显示"当前没有大脑"
- `CommandCenter` 新增 `/role` 命令路由
- 架构简化：放弃三层解耦（Brain → Role → Individual），改为两层（Brain → Individual）
- 构建通过

---

## 2026-04-27 21:28 [02-UI]

### /brain 命令 + 大脑配置弹窗

- 实现命令驱动交互：输入 `/brain` + Enter 弹出大脑配置窗口
- 新建 `CommandCenter` 客户端组件：接管输入框，检测命令
- 新建 `BrainModal` 客户端组件：空状态（+ 按钮）→ 表单状态（6 个配置字段）
- 手绘风格一致，保存/取消按钮，Escape / 遮罩点击关闭
- 构建通过

---

## 2026-04-27 22:36 [02-UI]

### UI 设计 + Subagent 调度首测 + 首页重构

- 确定设计基调：手绘简笔画风格，反差感（不"像 AI"）
- 创建 `docs/project/ui-设计.md`（v1）→ 重构为 v2
- 创建 `docs/claude-use/agents-机制说明.md` — 主/子 agent 调度机制说明
- 日志规则更新：署名改为 session 名称，格式调整为 `YYYY-MM-DD HH:mm [session]`
- 首测 subagent 调度：dispatch 一个 general-purpose agent 实现完整首页（v1，三按钮布局）
  - 产出 6 个文件，构建通过零错误
  - 节省主 context 约 5k-8k tokens
- 并行调度双 subagent（后台模式）：
  - **A — 首页视觉强化**：灰白素描纸背景、线条加粗、新增炭笔颗粒滤镜
  - **B — 人才库弹窗**：手绘风格弹窗 + 加号按钮，开闭动画，Esc 关闭
- **首页完全重构为 v2（命令驱动方向）**：
  - 参考 `docs/project/Gemini 沟通.md` 的设计方向
  - 新配色：`#E8E6E1` 暖灰纸色 + `#60100B` 牛血红
  - 新布局：右上小 OpenRise + 居中大 OpenRise（3 层炭笔重叠）+ 命令输入框 + `+` 按钮
  - 删除旧组件：PaperBackground、HeroLogo、HandDrawnButton、TalentModal（共 4 个文件）
  - 新增 SvgFilters 组件，精简为仅两个滤镜（tremble + charcoal）
- `docs/ui-设计.md` 移入 `docs/project/`，删除空目录 `docs/stitch/`

---

## 2026-04-27 17:45 [01-initial]

### 数据库配置完成（Steps 4-5）

- 执行 `npx prisma migrate dev --name init_openrise` 成功，生成 `prisma/dev.db` 和迁移文件
- 发现 Prisma v7 的 `prisma-client` 生成器只输出 TS 文件，CJS 无法加载
  - 解决方案：改用 `prisma-client-js` 生成器（v7 仍兼容），输出标准 JS
  - `@prisma/client` 的 `require()` 导入验证通过
- 创建 `main/db.js`，数据库路径按 `app.isPackaged` 动态切换（dev→`prisma/dev.db`，prod→`userData/openrise.db`）
- 文档同步更新：generator 说明、输出路径、引擎打包配置

---

## 2026-04-27 17:20 [01-initial]

### 重写数据库配置文档

- 重写 `docs/project-step/2-OpenRise 项目数据库配置.md`，按 5 个步骤组织
  - SQLite + Prisma 技术选型说明
  - Schema 三层模型 (Brain → Role → Individual → Message) 配深度解析
  - 外键字段加索引（`@@index`）
  - 数据库路径按 `app.isPackaged` 动态切换（统一第一步文档的写法）
  - 补充 Prisma 引擎在 electron-builder 中的打包注意事项
  - soul + rule 保持分开，作为产品特性保留

---

## 2026-04-26 22:40 [01-initial]

### 重构初始化指南文档

- 重写 `docs/project-step/1-OpenRise 项目架构与初始化指南.md`，按 7 个步骤重新组织
  - 原 Step 6（开发流配置）合并入 Step 4（package.json）的深度解析
  - 新增 Step 6：项目结构总览
  - 新增 Step 7：一键启动与开发验证（含启动序列详解、核心疑问解答）
  - Deep Dive 章节拆解融入各步骤：启动序列 → Step 7，安全隔离 → Step 3，Static Export → Step 5
  - 补充部分改为 H2 标题，保留端口冲突、Preload 局限性等内容
- 修复文档中 `electron-serve` 的 require 写法（v3 为 ESM 模块，需加 `.default`）
- 代码文件 `main/main.js` 中同步修复了该问题

---

## 2026-04-26 22:20 [01-initial]

### 项目初始化完成

- 在 `E:\Claude-code\openrise` 下完成 Next.js 16.2.4 脚手架搭建
  - 注意：`create-next-app` 的 "Use recommended defaults" 会跳过交互式选项，不会生成 `src/` 目录
  - 已手动将 `app/` 移入 `src/app/`，并修正 `tsconfig.json` 的 `@/*` 路径为 `./src/*`
- 安装 Electron 生态依赖：`electron`、`electron-builder`、`concurrently`、`wait-on`、`electron-serve`
- 创建 `main/main.js`（主进程）和 `main/preload.js`（预加载脚本）
- `package.json` 配置了 `main` 入口和 `dev`/`build` 等脚本
- `next.config.ts` 开启 `output: 'export'`（静态导出）+ 禁用图片优化
- `docs/project-step/1-OpenRise 项目架构与初始化指南.md` 已更新（增加推荐默认的避坑说明和手动修复方案）

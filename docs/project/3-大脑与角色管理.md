# 大脑与角色管理

## 大脑库（BrainLibrary）

全屏视图，通过侧边栏或 CommandCenter 的 brain 进入。

### 列表

卡片网格，`min-h-[110px]`，手绘描边：

```
{大脑名称} [{type}]       [⚡] [✏️] [✕]
{供应商} · {模型名}
```

- 测试按钮（⚡）：调用 `GET /models`，成功绿点、失败红点
- 编辑按钮（✏️）：进入表单，回填数据
- 删除按钮（✕）：弹出 ConfirmDialog 确认后删除，大脑删除后关联角色 brainId 置空（角色保留但无法使用）

### 表单

六个字段，手绘 shaky-line 底边：

| 字段 | 类型 | 说明 |
|------|------|------|
| 类型 | chat / image / vision 多选（可组合） | 选择大脑能力类型，支持多选。`chat` 用于对话/Agent，`image` 用于文生图，`vision` 用于多模态视觉。存储为逗号拼接字符串。 |
| 大脑名称 | input | 自定义名称 |
| 供应商名称 | input | 预设按钮快捷填充 |
| 请求地址 | input (url) | API 端点 |
| API Key | input (password) | 密钥 |
| 官网链接 | input (url) | 供应商官网 |
| 模型名 | input | 手动输入，如 `deepseek-chat` |

预设按钮：DeepSeek、智谱GLM、百炼、Kimi、硅基流动（点选后自动填入 vendor/endpoint/website）。

---

## 人才库（RoleLibrary）

全屏视图，通过侧边栏或 CommandCenter 的 role 进入。

### 列表

卡片网格，`min-h-[100px]`，手绘描边，左侧 AvatarIcon：

```
{AvatarIcon 44px}  {角色名称}        [✏️] [✕]
                   {关联大脑名称}
```

- 若关联大脑已被删除，大脑名显示红色 "大脑已删除"
- 编辑按钮（✏️）：进入表单，回填数据
- 删除按钮（✕）：弹出 ConfirmDialog 确认后删除，级联清理聊天消息 + Agent 会话

### 表单

| 字段 | 类型 | 说明 |
|------|------|------|
| 头像 | 8 个情绪 SVG | 3×3 网格选择器，点击即选即关闭 |
| 名字 | input | 必填 |
| 大脑选择 | select 下拉 | 列出所有可用大脑 |
| Soul | textarea | 人格设定，可选 |
| Rule | textarea | 行为规则，可选 |

### 头像

8 个涂鸦风情绪 SVG：cat、ghost、crown、robot、excited、lazy、cool、cute。
每个头像有独立配色和 CSS 动画（眨眼、浮动、弹跳等）。

---

## ConfirmDialog

自定义确认弹窗，替代浏览器原生 `window.confirm`。

- 手绘 SVG 边框 + 白色背景
- 标题、消息（`whitespace-pre-wrap` 支持多行）、确认/取消按钮
- `z-[100]` 确保悬浮在所有 UI 之上
- 点击遮罩层或取消按钮关闭

---

## IPC Handler

### Brain

| 通道 | 方法 | 说明 |
|------|------|------|
| `brain:list` | `prisma.brain.findMany()` | 按创建时间倒序 |
| `brain:create` | `prisma.brain.create()` | 含 name, provider, baseUrl, apiKey, modelName, type |
| `brain:get` | `prisma.brain.findUnique()` | 单个查询 |
| `brain:update` | `prisma.brain.update()` | 全字段更新 |
| `brain:delete` | `brainId 置空 → 删 Brain` | 不删角色，角色 brainId 设为 null |
| `brain:test` | `fetch /models` | 测试 API 连通性 |

### Role

| 通道 | 方法 | 说明 |
|------|------|------|
| `role:list` | `prisma.role.findMany({ include: { brain: true } })` | 含 brain 信息 |
| `role:create` | `prisma.role.create()` | 含 name, soul, rule, brainId, avatar |
| `role:update` | `prisma.role.update()` | 全字段更新 |
| `role:delete` | AgentSession(级联 AgentMessage) → Message → Role | 清理所有关联数据后删角色 |

---

## Schema 变更

| 模型 | 字段 | 变更 |
|------|------|------|
| Role | `brainId` | `String` → `String?`（可空） |

`brain:delete` 不再级联删除角色，改为将角色 brainId 置空。

---

## 前端 API 层（api.ts）

所有 IPC 调用通过 `src/lib/api.ts` 封装：

- 自动探测 `window.openriseAPI` 是否存在
- Electron 环境 → 走 IPC
- 非 Electron 环境 → 返回空数据（UI 不崩溃）

---

## 组件文件清单

| 文件 | 职责 |
|------|------|
| `components/BrainLibrary.tsx` | 大脑库全屏视图（列表 + 表单） |
| `components/RoleLibrary.tsx` | 人才库全屏视图（列表 + 表单） |
| `components/ConfirmDialog.tsx` | 自定义确认弹窗 |
| `components/AvatarIcon.tsx` | 头像 SVG + AVATARS 常量 |
| `src/lib/api.ts` | IPC 调用封装 |
| `shared/ipc-contracts.js` | IPC 通道合约 |

# 9. 发行流程和相关理论

> GitHub Release CLI 发行的重要理论知识 + 当前项目发行就绪状态评估。
>
> **更新日期：2026-06-07**

---

## 一、发行就绪评估

### 当前状态

| 检查项 | 状态 | 说明 |
|--------|------|------|
| `npm run build` | ✅ 可通过 | Next.js 静态导出 → electron-builder 打包 |
| 数据库初始化 | ✅ 正常 | `prebuild` 生成 `schema.sql`，首次启动自动建表 |
| 代码质量 P0/P1/P2 | ✅ 已解决 | 阻塞级和高优先级问题已全部修复 |
| P3 低优项 | ⚪ 4.2/4.5 保留 | 经评估不修改，不影响发行 |
| License | ✅ MIT | README 已声明 |
| README | ✅ 完整 | 功能、用法、技术栈、项目结构均已覆盖 |
| `.gitignore` | ✅ 正确 | `.env*`、`*.db`、`/out/`、`/.next/` 均已忽略 |

### 发行前需要补充的

| 缺失项 | 优先级 | 说明 |
|--------|--------|------|
| **electron-builder 配置** | ✅ 已补充 | `electron-builder.yml` 添加了 `appId`、`productName`、`files`（显式包含 `schema.sql`）、平台目标 |
| **版本号规范** | 🟡 建议 | 当前 `package.json` 中 `version: "0.1.0"`，语义明确可保留 |
| **发布清单** | 🟡 建议 | 确保 `schema.sql` 被 electron-builder 打包进安装包（依赖 `files` 配置） |
| **CHANGELOG** | 🟢 可选 | 初次发行可以没有，v0.2.0 起建议维护 |

---

## 二、前置准备：安装 GitHub CLI

### 2.1 检查是否已安装

```bash
gh --version
```

如果输出 `command not found` 则未安装。

### 2.2 安装方法

**Windows（winget）：**

```bash
winget install GitHub.cli
```

**macOS（Homebrew）：**

```bash
brew install gh
```

**Linux（apt）：**

```bash
sudo apt install gh
```

**验证安装：**

```bash
gh --version
# → gh version 2.x.x
```

### 2.3 登录认证

```bash
gh auth login
```

根据提示选择：
1. 登录方式：选 **GitHub.com**
2. 认证方式：选 **Login with a web browser**
3. 浏览器中复制 one-time code 完成授权

**验证登录：**

```bash
gh auth status
# → ✓ Logged in to github.com as <your-username>
```

> 需要仓库的 push 权限。如果 token 有效期过期，`gh release create` 会报 401 错误，重新 `gh auth login` 即可。

---

## 三、GitHub Release CLI 理论

### 3.1 什么是 GitHub Release

GitHub Release 是基于 Git tag 的「发布快照」。每个 Release 关联一个 tag，附带：

- **标题**（Title）：人类可读的版本名
- **发布说明**（Release Notes）：Markdown 正文，描述本次变更
- **附件**（Assets）：二进制文件（exe、dmg、zip 等），用户直接从 Release 页面下载

Release 不是 Git 原生概念——底层只是一个 tag + GitHub 附带的元数据。没有 GitHub 也可以 git tag，但 Release 提供了分发入口。

### 3.2 `gh release create` 完整命令

```bash
gh release create <tag> \
  --title "<标题>" \
  --notes "<正文>" \
  [--notes-file <文件>] \
  [--prerelease] \
  [--draft] \
  <assets...>
```

**参数详解：**

| 参数 | 作用 | 示例 |
|------|------|------|
| `<tag>` | 创建的 Git tag 名，必须遵守 semver | `v0.1.0` |
| `--title` | Release 页面显示的大标题 | `"OpenRise v0.1.0"` |
| `--notes` | 直接写发布说明（适合短内容） | `"首次发布"` |
| `--notes-file` | 从文件读发布说明（适合长内容） | `./CHANGELOG.md` |
| `--prerelease` | 标记为预发布版，不推送给"稳定版"用户 | |
| `--draft` | 草稿模式，只你可见，确认无误后手动发布 | |
| `<assets>` | 要上传的文件，支持 glob 通配 | `./dist/*.exe ./dist/*.dmg` |

### 3.3 执行流程

```
git tag v0.1.0  ──────┐
                       │
gh release create v0.1.0 ───→ 1. 创建 tag（如果还没推）
                               2. 上传 assets
                               3. 生成 Release 页面
                               4. 可选：自动发布到 GitHub 仓库首页
```

⚠️ **重要行为**：`gh release create` 会在本地创建 tag 并推送到远程。如果本地 tag 已存在，会报错。需要覆盖时用 `--force`。

### 3.4 查看和管理 Release

```bash
# 查看所有 Release
gh release list

# 查看某个 Release 详情
gh release view v0.1.0

# 删除 Release（tag 会保留，需要额外删除）
gh release delete v0.1.0

# 修改 Release 信息
gh release edit v0.1.0 --title "新标题"

# 下载 Release 中的某个 asset
gh release download v0.1.0 -p "*.exe"
```

---

## 四、语义化版本（SemVer）

### 3.1 格式

```
v主版本.次版本.补丁
```

| 分段 | 递增时机 | 示例 |
|------|----------|------|
| **主版本** | 不兼容的 API 改动 | 1.0.0 → 2.0.0 |
| **次版本** | 向下兼容的功能新增 | 1.0.0 → 1.1.0 |
| **补丁** | 向下兼容的问题修复 | 1.0.0 → 1.0.1 |

### 3.2 V1 前（0.x）

`0.x` 阶段不受 SemVer 约束。`0.1.0` → `0.2.0` 可以包含任意变更，用户预期就是不稳定的。当前版本 `0.1.0` 正是此阶段。

### 3.3 预发布标签

```
v1.0.0-alpha.1
v1.0.0-beta.1
v1.0.0-rc.1
```

`--prerelease` 标记的 Release 不会出现在"Latest Release"中，适合发给测试用户。

---

## 五、构建产物与发布资产

### 4.1 当前构建流程

```
npm run build
  ├── prebuild: prisma migrate diff ... > prisma/schema.sql  ← 生成建表 SQL
  ├── next build: 静态导出到 out/                              ← 前端页面
  └── electron-builder: 打包整个应用                            ← 安装包
```

### 4.2 electron-builder 打包什么

electron-builder 默认行为：

1. 将 `main.js` 所在的整个目录（即整个项目根目录）扫描
2. 排除 `.gitignore` 中列出的文件
3. `package.json` 中的 `dependencies`（不是 devDependencies）会打包进去
4. Electron 二进制 + 你的代码 → 输出 `dist/` 目录下的安装包

**关键风险**：`prisma/schema.sql` 是在 `prebuild` 中生成的，位于项目根目录下的 `prisma/` 中。默认情况下 electron-builder **不会排除** `.sql` 文件，所以它会被打包进去——这也是 `main.js:55` `path.join(__dirname, '..', 'prisma', 'schema.sql')` 能读取到的原因。

但**最佳实践**是在 electron-builder 配置中显式声明 `files` 来确保 `schema.sql` 被包含：

```yml
# electron-builder.yml（目前不存在）
files:
  - "**/*"
  - "!node_modules/**/*"
  - "!src/**/*"
  - "!docs/**/*"
  - prisma/schema.sql         # ← 显式包含
```

当前没有配置，完全依赖默认行为——能工作但不可靠。

### 4.3 跨平台产物

| 平台 | 预期产物 | electron-builder 输出 |
|------|----------|-----------------------|
| Windows | `OpenRise Setup 0.1.0.exe` | `.exe`（NSIS 安装包） |
| macOS | `OpenRise-0.1.0.dmg` | `.dmg`（磁盘映像） |
| Linux | `OpenRise-0.1.0.AppImage` | `.AppImage`（便携应用） |

生成哪些产物取决于在哪个平台运行 `npm run build`。**不能跨平台打包**——Windows 上只能打包 Windows 安装包。

---

## 六、发布清单

### 5.1 首次发布前

- [ ] 补充 `electron-builder.yml` 配置（至少指定 appId、productName、files）
- [ ] 验证 `schema.sql` 确实被打包进了安装包
- [ ] 在有用户数据的情况下测试升级覆盖安装（不丢数据）
- [ ] 在**空白机器**上测试首次启动（没有 Node.js、没有 Prisma）
- [ ] 准备 Release Notes

### 5.2 每次发布的标准流程

```bash
# 1. 更新版本号
#    package.json 中的 version 字段

# 2. 构建
npm run build

# 3. 验证产物
ls dist/

# 4. 创建 GitHub Release（草稿模式先检查）
gh release create v0.1.0 \
  --title "OpenRise v0.1.0" \
  --notes-file /tmp/release-notes.md \
  --draft \
  ./dist/*.exe ./dist/*.dmg ./dist/*.AppImage

# 5. 在 GitHub 上检查 Release 页面，确认无误后取消草稿状态
```

### 5.3 发布后验证

1. 从 GitHub Release 页面下载安装包
2. 安装到没有开发环境的机器
3. 首次启动确认自动建表无报错
4. 测试核心流程：创建 Brain → 创建 Role → Chat 对话 → Agent 任务 → Debate 辩论

---

## 七、常见问题

### Q: `gh release create` 和 `git tag && git push && gh release create` 的区别？

`gh release create <tag>` **一步完成**：创建 tag、推送到 remote、创建 Release、上传 assets。单独 `git tag` + `git push` 只推送 tag，不创建 Release 页面。

### Q: 上传失败的 assets 可以追加吗？

可以。`gh release upload <tag> <files...>` 向已存在的 Release 追加附件。

### Q: 发布后发现严重 bug 怎么办？

SemVer 流程：修复后 bump patch 版本，发布 `v0.1.1`。Release 页面会显示为"Latest Release"，用户重新下载即可。旧版本保留在 Release 列表中供回滚。

### Q: `--draft` 和 `--prerelease` 的区别？

| 模式 | 谁可见 | 用途 |
|------|--------|------|
| `--draft` | 仅仓库管理员 | 发布前的最终检查 |
| `--prerelease` | 所有人 | 测试版、预览版，不标记为 Latest |

可以先 `--draft` 检查，确认后再手动取消草稿发布。
